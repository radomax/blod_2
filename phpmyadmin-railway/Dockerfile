FROM phpmyadmin/phpmyadmin

ENV PMA_HOST=${PMA_HOST}
ENV PMA_PORT=${PMA_PORT}
ENV PMA_USER=${PMA_USER}
ENV PMA_PASSWORD=${PMA_PASSWORD}
ENV PMA_ABSOLUTE_URI=https://phpmyadmin.up.railway.app/

EXPOSE 80
